version: 0.2

env:
  shell: bash
  variables:
    AWS_REGION: "eu-west-2"
    ACCOUNT_ID: "430006376054"

    API_REPO: "suganthy-api"
    UI_REPO: "suganthy-streamlit"

    API_CONTAINER_NAME: "suganthy-api"        # MUST match API task definition container name
    UI_CONTAINER_NAME: "suganthy-streamlit"   # MUST match Streamlit task definition container name

phases:
  pre_build:
    commands:
<<<<<<< HEAD
      - echo Logging in to Amazon ECR
      - aws ecr get-login-password --region $AWS_REGION | docker login --username AWS --password-stdin $ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com

  build:
    commands:
      - echo Build API image
      - docker build -f src/Dockerfile.api -t $API_REPO:latest .

      - echo Build UI image
      - docker build -f src/Dockerfile.app_streamlit -t $UI_REPO:latest .
=======
      - "set -euo pipefail"
      - 'echo "Logging in to Amazon ECR..."'
      - 'aws ecr get-login-password --region "$AWS_REGION" | docker login --username AWS --password-stdin "$ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com"'
      - 'TAG="$(echo "$CODEBUILD_RESOLVED_SOURCE_VERSION" | cut -c1-7)"'
      - 'echo "Using image tag: $TAG"'

  build:
    commands:
      - 'echo "Build API image..."'
      - 'docker build -f src/Dockerfile.api -t "$API_REPO:$TAG" .'

      - 'echo "Build UI (Streamlit) image..."'
      - 'docker build -f src/Dockerfile.app_streamlit -t "$UI_REPO:$TAG" .'
>>>>>>> origin/suganthy-main

      - 'echo "Tag images for ECR..."'
      - 'docker tag "$API_REPO:$TAG" "$ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com/$API_REPO:$TAG"'
      - 'docker tag "$UI_REPO:$TAG" "$ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com/$UI_REPO:$TAG"'

  post_build:
    commands:
      - 'echo "Push images to ECR..."'
      - 'docker push "$ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com/$API_REPO:$TAG"'
      - 'docker push "$ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com/$UI_REPO:$TAG"'

<<<<<<< HEAD
      - echo Writing imagedefinitions files
      - printf '[{"name":"mary-mlops-api","imageUri":"%s"}]' "$ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com/$API_REPO:latest" > imagedefinitions-api.json
      - printf '[{"name":"mary-mlops-ui","imageUri":"%s"}]' "$ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com/$UI_REPO:latest" > imagedefinitions-ui.json
      - cat imagedefinitions-api.json
      - cat imagedefinitions-ui.json

artifacts:
  files:
    - imagedefinitions-api.json
    - imagedefinitions-ui.json


=======
      - 'echo "Create image definitions..."'
      # For API service deploy action
      - 'printf ''[{"name":"%s","imageUri":"%s"}]'' "$API_CONTAINER_NAME" "$ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com/$API_REPO:$TAG" > imagedefinitions-api.json'
      # For Streamlit service deploy action
      - 'printf ''[{"name":"%s","imageUri":"%s"}]'' "$UI_CONTAINER_NAME" "$ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com/$UI_REPO:$TAG" > imagedefinitions-ui.json'

      # Default file name some ECS deploy actions expect (ONLY ONE SERVICE can use this unless you reconfigure actions)
      - 'cp imagedefinitions-api.json imagedefinitions.json'

      - 'echo "Artifacts created:"'
      - 'ls -la'
      - 'echo "imagedefinitions-api.json:" && cat imagedefinitions-api.json'
      - 'echo "imagedefinitions-ui.json:" && cat imagedefinitions-ui.json'
      - 'echo "imagedefinitions.json:" && cat imagedefinitions.json'

artifacts:
  files:
    - imagedefinitions.json
    - imagedefinitions-api.json
    - imagedefinitions-ui.json
>>>>>>> origin/suganthy-main
